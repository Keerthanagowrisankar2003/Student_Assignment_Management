spring.application.name=classroom_assignment_management
# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/
spring.datasource.username=server_database
spring.datasource.password=Keerthanag@2003
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Port
server.port=8085

# JWT Secret Key (for simplicity, hardcoded here; in production, use environment variables)
jwt.secret=YourJWTSecretKey
jwt.expiration=86400000 
